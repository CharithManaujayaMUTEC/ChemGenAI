# ChemGenAI
This project develops an AI system to generate new chemical molecules with desired properties using a Conditional Variational Autoencoder (CVAE). Molecules are represented using SMILES notation and encoded into a latent space, enabling the generation of novel and chemically valid structures.
